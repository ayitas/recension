# Recension Web

SvelteKit dashboard for Recension.

## Requirements

- Svelte `5.56.4`
- Node 20+

## Develop

```bash
npm install
npm run dev
```

The Vite dev server proxies `/v1` and `/healthz` to `http://localhost:8080`.
